<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.samples.apps.cloudlaunch" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".gitkit.LoginActivity"
            android:label="GitKit"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="gitkit"
                    android:scheme="com.googleusercontent.apps.861206674703-hodei771leovo34b0p4th73pibv1dl7p" />
            </intent-filter>
        </activity>
        <activity android:name="com.facebook.LoginActivity" />

        <meta-data
            android:name="identitytoolkit.show_providers"
            android:value="Google, Yahoo, Facebook" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="identitytoolkit.api_key"
            android:value="AIzaSyDkpRSLeh3G_gB8d6-jZKHgsHecKMd3HxQ" />
        <meta-data
            android:name="identitytoolkit.server_client_id"
            android:value="861206674703-hodei771leovo34b0p4th73pibv1dl7p.apps.googleusercontent.com" />
        <meta-data
            android:name="identitytoolkit.server_widget_url"
            android:value="https://localhost/callback" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".BaseActivity"
            android:label="@string/title_activity_navigation_drawer" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".FeedActivity" >

            <!--
            This intent filter indicates to the system that this is the activity that controls your application's data usage.
            Thus, when the user inspects how much data your app is using from the system's Settings app, a View application
            settings button is available that launches this activity
            -->
            <intent-filter>
                <action android:name="android.intent.action.MANAGE_NETWORK_USAGE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.google.samples.apps.cloudlaunch.MainActivity" />
        </activity>
        <activity
            android:name=".FeedActivity"
            android:label="@string/title_activity_feed" >
        </activity>
    </application>

</manifest>
